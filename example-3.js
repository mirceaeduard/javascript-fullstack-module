/*
 Number
*/

// Display the total numbers of apples and grapes on the screen. And the difference between them.

const apples = 40;
let grapes = 50;

// grapes = grapes + 1; -> 51
// grapes += 1; -> 51
// grapes++;

const sum = apples + grapes;
const difference = apples - grapes;


const exampleMath = (100 + 30) - (2 * 5);

let value = 27.2;


// Math.round(value) -> rotunjeaza numarul corespunzator dpdv matematic 
// Math.ceil(value) -> rotuneza numarul intotdeauna in sus -> la cer = ceil
// Math.floor(value) -> rotunjeaza numarul intotdeauna in jos -> la floor = la pamant

console.log(`Valoare: ${Math.round(value)}`)

